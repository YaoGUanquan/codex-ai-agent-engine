---
name: ae:prototype-preview
description: Convert a PRD prototype document into technology-agnostic multi-page static HTML for prototype validation.
argument-hint: "[prd directory|prototype document] [--no-inspect|--yes]"
---

# Prototype Preview

Create a static HTML prototype from the supplied prototype document. Keep the result technology-agnostic and validate browser behavior only when the user explicitly requests it through `ae:playwright`.
