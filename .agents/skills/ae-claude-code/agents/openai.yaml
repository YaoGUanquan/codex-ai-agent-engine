interface:
  display_name: "AE Claude Code"
  short_description: "将本地 Claude Code CLI 作为受控外部 worker 使用"
  default_prompt: "使用 $ae-claude-code 将受限分析或 patch 候选方案委派给本地 Claude Code CLI。"
policy:
  allow_implicit_invocation: true
