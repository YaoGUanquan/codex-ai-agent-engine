interface:
  display_name: "AE PRD (ae-prd) / AE PRD (ae-prd)"
  short_description: "ae-prd 在实现计划前沉淀产品需求和验收边界 / ae-prd captures product requirements before implementation planning"
  default_prompt: "使用 $ae-prd 在计划前定义这个需求。 / Use $ae-prd to define the requirement before planning."
policy:
  allow_implicit_invocation: true
