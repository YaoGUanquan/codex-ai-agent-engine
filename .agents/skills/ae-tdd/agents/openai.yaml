interface:
  display_name: "AE 测试驱动开发 / AE TDD"
  short_description: "以先失败测试为起点执行红绿重构循环 / Run a red-green-refactor loop with a failing test first"
  default_prompt: "使用 $ae-tdd 以先写失败测试的方式实现这个改动。 / Use $ae-tdd to implement this change with a failing test first."
policy:
  allow_implicit_invocation: true
