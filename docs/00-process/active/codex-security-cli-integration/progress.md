# Codex Security CLI Integration Progress

- 2026-07-31: Extended the approved scan workflow with a self-hosted Provider handoff. The plugin never reads Codex configuration or credentials; a user-owned opaque launcher controls the compatible provider and model.
- 2026-07-31: Defined `docs/ae/security-scans/` as a location for sanitized summaries. With explicit user authorization, raw scan artifacts may remain locally in `.private/` only after Git-ignore verification; protected external storage remains the default.
- 2026-07-31: Source/mirror contract tests, full test suite, distribution smoke, static checks, and implementation/document review completed without blocking findings. Target projects enable local raw retention only through a separately approved `.gitignore` change.
- 2026-07-31: Confirmed the revised local-memory model: complete original unredacted CLI artifacts remain in the explicitly approved, Git-ignored `.private/<scan-id>/` directory; the normal summary is a sanitized index. Retrieval for user-requested inspection or remediation does not rescan, while a rescan is required only to validate the post-remediation source state.
- 2026-07-31: Validated the retention revision with focused security-scan tests, source/mirror and skill-contract checks, `git check-ignore` canary verification, `npm.cmd test` (93/93), and `npm.cmd run check`. Document review found no blocking coherence, feasibility, traceability, or evidence-integrity issue.
- 2026-07-31: Added the self-hosted launcher directory-bootstrap contract. An explicit initialization request now creates only a placeholder `codex-security-newapi-launcher.ps1` in a user-owned external directory when that exact file is absent; it never enumerates, reads, validates, or overwrites launcher contents, and the user must fill provider settings before the launcher becomes opaque.
- 2026-07-31: Validated directory bootstrap with the focused security-scan test set, PowerShell parser validation of the distributed launcher template, source/mirror and skill-contract checks, `npm.cmd test` (93/93), `npm.cmd run check`, and a scoped review. One fixed-name collision wording issue was found and corrected before this final validation.
- 2026-07-31: Replaced the inferred `$5` cap with a mandatory per-scan cost decision for every source-analyzing scan. The workflow now estimates local eligible-source bytes and tokens, discloses pricing uncertainty, requires `no cap` or a user-selected USD cap, emits `--max-cost` only for that explicit cap, and records estimate, choice, and CLI-reported cost in the sanitized summary.
- 2026-07-31: The user-authorized full-repository self-hosted scan selected `no cap` and ended inconclusively after the CLI exhausted connection retries with a request timeout. No completed coverage, cost, or findings result exists; no raw output was copied into the repository and no automatic retry was started.
- 2026-07-31: With a fresh explicit `no cap` choice, three identical small Canary requests targeting only `plugins/ai-agent-engine-codex/skills/ae-security-scan` exited in about one second with code `2`. Each external output directory contains only a local console log and no scan artifacts; the CLI log contents remain unread, and no launcher or credential was inspected.
- 2026-07-31: Diagnosed the Canary preflight and transport boundary. The first three local failures were caused by a nonempty `--output-dir`. The corrected request reached Security CLI scan execution but repeatedly lost its Codex connection without New API records. Installed CLI documentation confirms its isolated runtime does not recognize `OPENAI_BASE_URL`; a local `--dry-run` confirmed the Windows `.cmd` wrapper accepts `--codex openai_base_url='...'` with TOML single quotes, while double quotes are stripped and rejected. The distributed placeholder now uses the supported override; the populated user launcher remains untouched.
- 2026-07-31: After explicit user authorization, updated the populated external launcher without displaying or copying provider values. Its syntax and no-network dry run passed. One new no-cap Canary then reached New API, proving the Base URL override was applied, but Security CLI repeatedly reported an interrupted Codex connection and exited `2`. The external output contains an unread original-artifact tree; no raw output was copied into the repository. Further scans are deferred because the provider's Responses/streaming compatibility remains unverified.
- 2026-07-31: User-provided New API evidence showed a successful `/v1/responses` call with `200`, 7-second total latency, and `$0.01504` upstream charge. Temporary no-source probes through the authorized launcher returned `200 text/event-stream` with `response.created`, `response.completed`, `response.output_item.done`, and `response.function_call_arguments.delta` for both text and function-call cases. The temporary probe code was removed and the launcher passed parser and dry-run checks; Codex-specific request/event compatibility remains the unresolved boundary.
- 2026-07-31: A new no-cap Canary against `plugins/ai-agent-engine-codex/skills/ae-security-scan` completed with CLI exit `0`, complete coverage, zero CLI-reported findings, and CLI-estimated cost `$1.6135095` (3,170,637 input tokens, 2,990,848 cached, 27,755 output). Original artifacts were retained only after `git check-ignore` verification under `.private/codex-security-canary-final-01/`; the sanitized summary is `docs/ae/security-scans/2026-07-31-ae-security-scan-canary-summary.md`.
- 2026-07-31: The user-authorized no-cap full-repository scan reached startup and discovery workers but exited `2` after repeated `Codex connection interrupted` retries. The terminal CLI diagnostic was `The configured account reached its rate limit. Wait and retry.` No coverage, findings, or CLI cost estimate was produced. The incomplete original artifacts were retained under `.private/codex-security-full-final-01/` after ignore verification, with a sanitized incomplete summary at `docs/ae/security-scans/2026-07-31-ae-security-scan-full-incomplete-summary.md`.
- 2026-07-31: Full-scan discovery artifacts showed six delegated worker files. Because the isolated Codex session cap includes the parent thread, the launcher and distributed template now set `features.multi_agent_v2.max_concurrent_threads_per_session=6`, allowing at most five delegated workers. This reduces concurrency pressure but does not by itself remove upstream account rate limits.
