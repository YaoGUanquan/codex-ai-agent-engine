# Personal Marketplace Global Plugin Progress

- 2026-08-10: User approved continuing in the existing dirty `main` worktree.
- Scope: publish AE through the personal Codex marketplace and official CLI; retain the private dispatcher; do not scan or clean consumer projects without a manifest.
- Evidence: `codex plugin marketplace list` now shows `personal` rooted at `C:\Users\yaogu`, and `codex plugin list --marketplace personal` reports `ai-agent-engine-codex@personal` as `installed, enabled, 0.3.16`.
- 2026-08-10: Current-user apply `9323637c-eea6-4509-8e9a-90ca2c151dfa` completed the initial publication; its backup contains the retired `ae-user-discovery-probe-20260810`. Upgrade operation `bb3fa708-be84-41a7-987e-5ce2576429a3` completed the `0.3.16` recovery-lifecycle fix. No consumer manifest was supplied, so no project root was scanned or changed.
- 2026-08-10: The observed CLI requires `codex plugin marketplace add $HOME --json` because it resolves `$HOME/.agents/plugins/marketplace.json` under the supplied root. Direct `codex plugin add` before this registration fails with `plugin ... was not found in marketplace personal`; the installer now performs both supported CLI steps and journals each status.
- 2026-08-10: Final gate passed at `docs/ae/gates/20260810T102232Z-work-final.json`. Full validation: `npm.cmd test` 105/105, `npm.cmd run check`, global smoke, release notes, AE artifacts, design contract, and `git diff --check` all passed.
- 2026-08-10: Final code/document review found and fixed one P1 recovery-lifecycle defect: purge now rejects `recovery-failed` until explicit recovery. Reviewer and architect lanes have no remaining blocking findings; current-user source and installed plugin fingerprints match.
- 2026-08-10: Migrated `D:\codes\ph-ustyle\ustyle` with explicit manifest and `--retire-modified`. Operation `977ed487-153b-4fc2-aa54-613d8e9947d5` completed with 54 backed-up changes. The local `0.3.0` plugin, wrappers, skills, and project marketplace AE entry are retired; `docs/**` and `AGENTS.md` fingerprints match preflight, and the project worktree remains clean.
- 2026-08-10: Migrated the approved seven consumers with explicit manifest and `--retire-modified`. Operation `39b29a4a-426f-447c-b16b-abac891e6820` completed with 312 backed-up changes. All seven have no local plugin, wrapper, AE skills, or AE marketplace entry; all `docs/**` and `AGENTS.md` preflight fingerprints match.
- 2026-08-10: Consumer dispatcher smoke passed for all eight migrated projects. The only remaining project-level AE copies are the intentionally retained distribution source `D:\codes\ph-AI-Agent-Engine` and excluded `D:\codes\work`.
- 2026-08-10: Documentation and durable memory now record the portable manifest command, `--retire-modified` backup-before-retirement authorization, project-local docs/graph ownership, personal-plugin discovery surfaces, and declared knowledge relations. Read-only verification shows the current Codex registration is enabled at `0.3.16`; repository source is `0.3.17`, so a separate global apply is required to upgrade the installed plugin.
