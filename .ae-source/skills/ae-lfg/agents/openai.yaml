interface:
  display_name: "AE 全流程 (ae-lfg) / AE LFG (ae-lfg)"
  short_description: "ae-lfg 从需求到验证交付的完整 AE 流程 / ae-lfg full AE workflow from requirement to verified delivery"
  default_prompt: "使用 $ae-lfg 将这个软件需求推进到计划、实现、审查和验证。 / Use $ae-lfg to take this software requirement through plan, work, review, and validation."
policy:
  allow_implicit_invocation: true
