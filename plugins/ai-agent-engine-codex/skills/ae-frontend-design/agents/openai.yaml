interface:
  display_name: "AE 前端初版 / AE Frontend Design"
  short_description: "按现有前端栈交付可用的首版界面体验 / Build a usable first frontend version with browser validation"
  default_prompt: "使用 $ae-frontend-design 为这个功能做一个可用的前端初版。 / Use $ae-frontend-design to build a usable first frontend version."
policy:
  allow_implicit_invocation: true
