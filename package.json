{
  "name": "ai-agent-engine-opencode",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": ".opencode/plugins/ae-server.js",
  "description": "Project-local OpenCode runtime fork of AI Agent Engine, excluding PDF and Office document capabilities.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/postbuild.mjs",
    "test:runtime": "vitest run",
    "test:codex-legacy": "node --test tests/*.test.mjs",
    "test": "npm run test:runtime",
    "test:slow": "vitest run --config vitest.slow.config.ts",
    "test:e2e": "npm run build && vitest run --config vitest.e2e.config.ts",
    "test:all": "npm run test && npm run test:e2e",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "opencode:upstream-check": "node scripts/check-opencode-upstream.mjs",
    "check": "npm run typecheck && node scripts/check-opencode.mjs && node scripts/check-skill-mirror.mjs && node scripts/check-skill-language-metadata.mjs && node scripts/check-skill-contract.mjs && node scripts/check-install-smoke.mjs && node scripts/check-ae-artifacts.mjs && node scripts/check-design-contract.mjs",
    "help": "node scripts/ae-tools.mjs help"
  },
  "dependencies": {
    "@alibaba-group/open-code-review": "^1.7.14",
    "@opencode-ai/plugin": "1.18.4",
    "@opencode-ai/sdk": "1.18.4",
    "@playwright/cli": "^0.1.17",
    "effect": "4.0.0-beta.100",
    "strip-json-comments": "^5.0.3",
    "tree-sitter-go": "^0.25.0",
    "tree-sitter-java": "^0.23.5",
    "tree-sitter-javascript": "^0.25.0",
    "tree-sitter-python": "^0.25.0",
    "tree-sitter-typescript": "^0.23.2",
    "web-tree-sitter": "^0.26.11",
    "yaml": "^2.8.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "esbuild": "^0.28.1",
    "typescript": "7.0.2",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "uuid": "^11.1.1",
    "zod": "^4.4.3",
    "effect": "4.0.0-beta.100"
  }
}
